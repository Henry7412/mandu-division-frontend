import { Routes } from '@angular/router';
import { DivisionListComponent } from './Infrastructure/Components/division-list/division-list.component';

export const routes: Routes = [
    {
        path: '',
        component: DivisionListComponent,
    },
];
